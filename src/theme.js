import { createTheme } from "@mui/material";
import React from "react";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#373E98",
            light: "skyblue",
            background: "#e0ecf8",
        },
        secondary: {
            main: "#FAE6B1",
        },
    },
});
